import React from "react";
import "./propertyButtons.css";

const testButton = props => (
    <button type="button" className="btn btn-primary" {...props}>Create New Unit</button>
)
export default testButton;